<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/background_light" />  <!-- Color de fondo -->
    <corners android:radius="8dp" />  <!-- Esquinas redondas -->
    <stroke
        android:width="1dp"
        android:color="@color/on_surface_variant" />  <!-- Borde -->
</shape>